# Review for Claude Code — 17 August 2026

Scholar evaluation of `paper/main.tex` (HEAD `d51caff` when written; working
tree may have moved). HTML twin: `REVIEW-2026-08-17.html`.

**Verdict: 4/5.** The science is sound. Do not reopen the design. Fix
internal inconsistency: stale limitation prose, a duplicate label, an
uncited instrument, one wrong macro used as a scale ratio, and four
typed numbers that violate the paper's own rule.

This file is the job list. Implement the **Must fix** items in one pass.
Do not invent new results. Do not type a measured number into `main.tex`.

## Standing rules (this repo)

- Every measured number in `paper/main.tex` is a macro from
  `scripts/paper_numbers.py` → `paper/numbers.tex`. A typed result is a
  bug, even if the figure is currently correct.
- After any number change: `python3 scripts/paper_numbers.py` then
  `python3 scripts/claims.py` then `python3 scripts/lint_paper.py`.
- Build: `cd paper && tectonic -X compile main.tex --outdir .`
- Cite Schwartz 1992 at first use of the circumplex. The 2012 overview
  is the entry point, not the source. See `REFERENCES.md` §Schwartz.
- Do not pool hosted cells into the 9-model mean.
- Do not restore the withdrawn clustering / raw-flip-count claims.

## Must fix

### 1. Duplicate `\label{sec:strength}`

- `paper/main.tex:677` — subsection "Direction survives; strength does
  not" (keep this label; it is the mechanism).
- `paper/main.tex:1622` — section "How much to believe each of these"
  (rename the label, e.g. `sec:weight` or `sec:believe`).
- Abstract ~line 98 says `\S\ref{sec:strength}` "ranks all of them by
  how much weight they bear". That pointer currently resolves to the
  mechanism figure. After the rename, point the abstract (and any other
  ranking refs) at the new label. Grep `sec:strength` and `ref{sec:`
  before declaring done.

### 2. Stale limitation / evidence-count prose

These sentences describe an earlier draft and now contradict the paper's
own tables.

| Location | Current text | True state |
|---|---|---|
| `main.tex` ~2138–2146 | Neutral control "on one model… open for the other eight" | `sec:neutral` / macros: `\NeutNModels=6`, `\NeutNCells=12`, `\NeutNInterp=3`, `\NeutNRefuse=3`. The one-model story is already the retraction inside `sec:neutral`. |
| `main.tex` ~2148–2153 | "Nine open-weight models between 0.8B and 9B… No frontier or closed model is included." | Same paper reports Llama-3.3-70B (3 seeds) and Qwen3-235B (1 seed) as hosted scale checks. Scope is: 9 self-hosted 0.8–9B **plus** hosted cells reported beside the ladder, not inside its mean. |
| `main.tex` ~1743–1748 | "Six claims are established… three are provisional… one is open." | `paper/table_claims.tex` is 13 established / 11 provisional / 0 open. `claims.json` has no `open` row. Count from the generated table, do not hard-type 13/11. |

Rewrite so each paragraph matches the section it summarises. Do not
delete the one-model retraction in `sec:neutral`; delete only the
limitations paragraph that pretends the expansion never happened.

### 3. `\NHosted` used as a scale multiplier

- `paper/main.tex` ~985: `Qwen3-235B … is \NHosted{}$\times$ the largest
  model in the self-hosted ladder and 3.4$\times$ the largest previously
  measured here.`
- `\NHosted` is **3** (count of hosted tiles with a floor). 235/9 ≈ 26,
  235/70 ≈ 3.4.
- Fix: add a dedicated ratio macro in `scripts/paper_numbers.py` (letters
  only in the name), or write the ratios as declared literals in
  `claims.json` if they are not generated. Do not leave `\NHosted` as
  the multiplier. The following "3.4×" is the 70B comparison and is
  roughly right; stop typing it if you can emit it.

`NHosted` (3 scored), `HostedNScoreable` (4, includes Qwen-30B with no
N− cell), and `HostedNScored` (3) are three different quantities. Do not
collapse them.

### 4. Cite Schwartz in the manuscript

- Used at `main.tex:69` (abstract, "Schwartz value circumplex") and
  `:1808` ("four Schwartz personas").
- `paper/refs.bib` already has `schwartz1992universals` (cite this at
  first mention), plus 1987/1990/1994/2012.
- None of those keys appear in `main.tex`. `REFERENCES.md` already
  says: whoever writes Track 6 / Schwartz into `main.tex` must carry
  the citation. That sentence is now due.
- Also unused in the bib: `perez2022discovering`,
  `durmus2023globalopinions`. Cite them at "Why not a personality
  inventory" (~line 380) or drop them from the bib.

### 5. Typed persona numbers

- `main.tex:1816–1817`: `+0.517` → `+0.258`, `90%` → `75%`.
- Source is a handoff note (`HANDOFF-NEXT.md` §5.B), not
  `numbers.tex`. They will rot the next time fig5 is rebuilt.
- Emit from `site/persona_denominator.json` / the fig5 rebuild path
  via `scripts/paper_numbers.py`. Letters-only macro names.
- Once they are macros, move one sentence of the empty-slot control
  from `sec:pretending` into `sec:persona`. The ledger
  (`persona-needs-an-empty-slot-control`) already says displacement
  must not travel without that denominator.

## Should fix

### 6. Split the six floor-clears

`NClears=6` mixes two facts. From `paper/table_main.tex`:

- Conviction-collapse clears: gemma-4-E2B-it, granite-4.1-3b,
  Qwen3.5-2B (and SmolLM3 / Qwen-4B / Qwen-9B fail the floor).
- Zero-conviction clears: SmolLM2-1.7B, LFM2.5-1.2B, Qwen3.5-0.8B —
  decisive fraction 0.0–0.1% on **both** arms. They are consistent
  about near-indifference, slightly more so on real text.

`claims.json` `strength-collapse` already notes `\MeanDecisiveR` is
not a 9-model mean. Mark the zero-conviction clears in the table
caption or split `NClears` into two macros. Do not change who
clears; change how the count is read.

### 7. Report the N+ arithmetic contrast

N+ is collected: 27 cells in `card.json`, `arithmetic_component` on
every tile, surface table already has `\SurfNpRsq`. Headline residual
is R − N− only. The method says claims about arithmetic are licensed
by N+ → N−. Either add an N+ column / one residual sentence, or
state that the arithmetic contrast is small and point at
`arithmetic_component`. Do not leave a designed factor with no
result.

### 8. Abstract pronoun

"6 of 9 clearing their own replicate floor and two falling below
**it**." Three fail `clears_floor`. Two have a negative residual
(N− scored higher). The ranking section (`sec:strength` at 1674+,
after you rename it) already says this correctly. Fix the abstract
to match.

### 9. Stale `grows_by` on `floor-holds-at-seventy-b`

`claims.json`: status `established`, evidence note says 3 seeds,
`grows_by` still says "one replicate is what keeps this provisional."
`table_claims.tex` lists this claim as "2 models"; it is one model
at 3 seeds. Update `grows_by`. Do not change the residual: +0.0083
does **not** clear the 0.0208 design floor.

## Nits (optional)

- Abstract hosted clause: `\HostedClears` expands to "does not
  clear". Write the verb into the sentence.
- Printed `0.906 − 0.880 = 0.026` but `\MeanResidual=+0.025`
  (unrounded means). One extra decimal, or one "unrounded" word.
- Title-page date is 16 August; several lead results landed on the 17th.

## What is already correct — do not "fix"

- Residual +0.025, CI [+0.0073, +0.0443], sign-test p = 0.18, t = 2.51.
  The claim is that the residual is **small**, not that it is zero.
- 70B: R 0.928, N− 0.920, residual +0.0083, design floor 0.0208,
  does not clear. 3 seeds. Hosted, so beside the ladder, never in
  the 9-model mean.
- 235B: residual −0.0235, 1 seed, no floor. Body already says this.
- MIXED retraction: 795 of 22,436 raw, 103 robust (13%), 3 models
  with none. Do not restore the raw count.
- Detector 0.596 kept vs 0.821 answer-mass is an **oracle**
  (arm labels in hand). Caption and ledger already say so.
- Surface R² 0.090 real vs 0.384 invented. Length is uncontrolled;
  that control is an upper bound, already labelled as such.
- Persona 14 of 20 conditions above +0.30 is from
  `site/persona_depth.json`, not `persona_denominator.json`.

## Verify before you stop

```bash
python3 scripts/paper_numbers.py
python3 scripts/claims.py
python3 scripts/lint_paper.py
cd paper && tectonic -X compile main.tex --outdir .
```

Grep after the label rename:

```bash
rg 'sec:strength' paper/main.tex
rg '0\.517|0\.258' paper/main.tex   # must be gone from prose
rg 'Schwartz' paper/main.tex        # must have a \\citep nearby
rg 'NHosted' paper/main.tex         # must not be a size ratio
```

Citation both-ways (cited-missing must be empty; unused Schwartz keys
should shrink):

```bash
grep -oE '\\cite[tp*]*\{[^}]+\}' paper/main.tex \
  | grep -oE '\{[^}]+\}' | tr -d '{}' | tr ',' '\n' | sort -u > /tmp/cited
grep -oE '^@[a-z]+\{[^,]+' paper/refs.bib | sed 's/.*{//' | sort > /tmp/have
comm -23 /tmp/cited /tmp/have
comm -13 /tmp/have /tmp/cited
```

## Out of scope

- Do not review or ingest `clustering science direct.pdf` unless asked.
  Clustering was withdrawn after a cluster-tendency test.
- Do not reopen Mazeika / Ashkinaze / Khan PDFs to chase 75.6% / 0.30 /
  0.770. Those are attributed in `REFERENCES.md`. Do not add new
  literature numbers.
- Do not commit unless the human asks.
